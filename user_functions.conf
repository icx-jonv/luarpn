{
    ['Parabola'] = function(stack)
        if #stack.stack > 0 then
            return stack.stack[#stack.stack].value^2
        end
    end,
    ['weird'] = function(stack)
        local st = stack.stack
        local count = #st
        if count > 1 then
            local a = st[count-1].value
            local b = st[count].value
            return a^2 + b
        end
    end
}
