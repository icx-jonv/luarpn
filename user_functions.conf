{
    ['Parabola'] = function(args)
        if #args > 0 then
            return args[1]^2
        end
    end,
    ['weird'] = function(args)
        local count = #args
        if count > 1 then
            local a = args[2]
            local b = args[1]
            return a^2 + b
        end
    end,
    ['AirHours'] = function(args)
        if #args > 1 then
            return args[1]*115
        end
    end,
}
